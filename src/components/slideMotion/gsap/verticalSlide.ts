import { animationSettings } from "../../../utils";
export const horizontalSlide = (
  currentSlide: any,
  upcomingSlide: any,
  tl: any,
  dir: string
) => {
  const times = {};
  // times.switchtime =
  //   action === "show"
  //     ? Number(
  //         animationSettings.staggerFactor * (currentSlide.length - 1) +
  //           animationSettings.duration
  //       )
  //     : Number(
  //         Math.max(
  //           0.05 + 0.04 * (currentSlide.titleLettersTotal - 1),
  //           animationSettings.duration
  //         )
  //       );
};
